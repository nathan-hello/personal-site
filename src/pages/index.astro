---
import BlogPost from '@/components/BlogImageBoard.astro'
import { globBlogs } from '@utils/glob'
import DefaultLayout from '@layouts/Root.astro'

const bothRecents = await globBlogs(100, undefined, true)
---

<DefaultLayout>
    {
        bothRecents.map((b) => {
            return (
                <BlogPost details={b.props.c}>
                    <b.props.c.Component />
                </BlogPost>
            )
        })
    }
</DefaultLayout>
